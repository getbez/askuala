
  <% if assessment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(assessment.errors.count, "error") %> prohibited this assessment from being saved:</h2>

      <ul>
      <% assessment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :concept_id %>
    <%= f.number_field :concept_id , class: 'hidden'%>
    <%= Concept.find(assessment.concept_id)&.title  %>
  </div>

  <div class="field">
    <%= f.label :level %>
    <%= f.collection_select :level, Assessment.levels, :first, :first, {:include_blank => '-- Select --'},{:class => 'form-control'} %>
  </div>

  <div class="field">
    <%= f.label :min_passing_grade %>
    <%= f.text_field :min_passing_grade %>
  </div>  

