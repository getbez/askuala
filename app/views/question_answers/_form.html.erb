<%= form_for(question_answer) do |f| %>
  <% if question_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question_answer.errors.count, "error") %> prohibited this question_answer from being saved:</h2>

      <ul>
      <% question_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :question_id %>
    <%= f.number_field :question_id, hidden: true %>
    <%= Question.find(question_answer.question_id).question_string %>
  </div>

  <div class="field">
    <%= f.label :choice_id %>
    <%= f.collection_select :choice_id, Question.find(@question_answer.question_id).choices, :id, :letter %>
  </div>

  <div class="field">
    <%= f.label :explanation %>
    <%= f.text_area :explanation %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
